---
description: タスクからテストケースを列挙し、人間承認を得る
---

## 引数説明

- **$Arguments[0]**: 仕様名（例: session-recovery-connector-update）
- **$Arguments[1]**: タスク ID（例: TASK-001）

## 実行手順

### 1. 進捗状況の確認

`task-progress.json`を確認し、対象タスクの現在の状態を把握します。

### 2. タスク情報の取得

`.specs/$Arguments[0]/tasks.md`から指定されたタスクの詳細情報を取得:

### 3. テストケース列挙プロセス

#### 3.1 要件分析

タスクの受入条件から以下を分析:

- **機能要件**: 実装すべき機能
- **制約条件**: 技術的制約
- **エラー条件**: 予想されるエラーシナリオ

#### 3.2 テストケース生成

以下の観点から網羅的にテストケースを生成:

##### 正常系テストケース

##### 異常系テストケース

##### エッジケーステストケース

#### 3.3 テストケース出力とファイル保存

生成されたテストケースを以下の形式で出力し、ファイルとして保存します。

##### テストケースファイルの保存

`.claude/templates/testcase.md`の形式に従い、`.specs/$Arguments[0]/testcases/$Arguments[1].md` を生成。

### 4. 進捗更新

テストケース列挙とファイル保存完了後、`task-progress.json`を更新:
